@{
    ViewBag.Title = "Биржа квадратных сантиметров";
}

<div class="form-group search-box">
    @using (Html.BeginForm("Results", "Search", FormMethod.Post))
    {
        @Html.AntiForgeryToken()
        <div class="col-md-7"><input class="form-control" name="name" type="text" placeholder="Владелец, Место, Адрес"></div>
        <div class="col-md-4 pull-right"><button class="btn btn-default" type="submit">Поиск</button></div>
    }
</div>
<div class="form-group">
    <h3>Топ-недвижимость</h3>
    <div>

    </div>
</div>
